// loading to be displayed when loading state is true
import React from 'react';

const Loading = () => {
  return (
    <div className='loading'>
      <h1>loading...</h1>
    </div>
  );
};

export default Loading;
